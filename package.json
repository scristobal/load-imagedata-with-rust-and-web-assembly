{
  "name": "load-imagedata-with-rust",
  "version": "0.0.0",
  "description": "A quick experiment to get raw pixels of a PNG using Wasm",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
